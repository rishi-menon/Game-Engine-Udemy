#pragma once

static const unsigned int g_cWindowWidth = 800;
static const unsigned int g_cWindowHeight = 600;

static const int g_cTargetFPS = 60;

//target delta time in milliseconds
static const float g_cTargetFrameDeltaTime = 1000.0f / (float)g_cTargetFPS;
